import 'package:flutter/material.dart';

class GuardVideoViewer extends StatelessWidget {
  static const routeName = '/guard-viewer';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
